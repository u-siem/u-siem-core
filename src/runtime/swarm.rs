/// Connecto the local runtime with the cluster.
/// 
/// If a service is not found in the local runtime, then the command is forwarded to another runtime in the cluster
pub trait SwarmConnector {
    
}